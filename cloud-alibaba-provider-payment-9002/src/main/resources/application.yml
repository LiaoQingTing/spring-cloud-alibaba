server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: liaoqingting.com:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
